import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.espeu.app',
  appName: 'prestamoPeliculas_frontend',
  webDir: 'dist/prestamo-peliculas-frontend',
  bundledWebRuntime: false
};

export default config;
